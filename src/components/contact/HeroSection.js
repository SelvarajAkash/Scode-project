import React from "react";
import {
  HeroContainer,
  HeroContent,
} from "../../HeroElements";
import "../../App.css";

const HeroSection = () => {

  return (
    <HeroContainer>
      <HeroContent>
        <h1 className="h1-hero">Contact</h1>

      </HeroContent>
    </HeroContainer>
  );
};

export default HeroSection;
